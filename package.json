{
  "name": "challenge-escrow-protocol",
  "version": "0.1.0-research",
  "private": true,
  "description": "My research reference implementation for equal-stake, non-custodial challenge escrow.",
  "license": "MIT",
  "packageManager": "pnpm@10.29.1",
  "scripts": {
    "build": "forge build --root contracts",
    "fmt:check": "forge fmt --root contracts --check",
    "vectors:check": "node tools/verify-vectors.mjs",
    "schemas:check": "node tools/portable/schema-check.mjs",
    "portable:check": "node tools/portable/verify.mjs && python3 tools/portable/verify_portable.py",
    "client:check": "deno check tools/client/index.ts tools/client/test.ts tools/client/observer.ts tools/client/observer.test.ts && deno run tools/client/test.ts && deno run tools/client/observer.test.ts",
    "simulator:test": "node tools/simulator/run.mjs --all",
    "liveness:sweep": "node tools/liveness/sweep.mjs",
    "failure:lab": "node tools/liveness/failure-lab.mjs",
    "authority:v2": "node tools/liveness/authority-v2.mjs",
    "check:line5": "pnpm run check && pnpm run liveness:sweep && pnpm run failure:lab && pnpm run authority:v2",
    "model:test": "node tools/model/run.mjs",
    "formal:check": "node tools/formal/check.mjs",
    "size:check": "node tools/security/size-check.mjs",
    "security:baseline": "node tools/security/attack-baseline.mjs",
    "security:mutation": "node tools/security/mutation.mjs",
    "medusa:test": "node tools/medusa/run.mjs",
    "audit:baseline": "node tools/security/audit.mjs",
    "test": "forge test --root contracts -vv",
    "test:security": "FOUNDRY_PROFILE=security forge test --root contracts -vv",
    "check": "pnpm run fmt:check && pnpm run vectors:check && pnpm run build && pnpm run test:security",
    "check:line1": "pnpm run check && pnpm run model:test && pnpm run formal:check",
    "check:line2": "pnpm run audit:baseline",
    "check:line3": "pnpm run check && pnpm run schemas:check && pnpm run portable:check",
    "check:line4": "pnpm run check && pnpm run client:check && pnpm run simulator:test",
    "audit:dependencies": "pnpm audit --audit-level high"
  },
  "engines": {
    "node": ">=22.15.1"
  }
}
